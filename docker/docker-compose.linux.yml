version: "3.9"
services:

  daemon:
    environment:
      DOCKER_HOST: null
    volumes:
      # On linux we mount the docker socket directly!
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
      - type: bind
        source: ../scripts/
        target: /scripts/
